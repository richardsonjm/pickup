var td = $('#membership-status-for-game-<%= @membership.game.id %>')
td.find('a').remove()
td.append('<%= escape_javascript(render partial: "memberships/leave_link", locals: {membership: @membership}) %>')
