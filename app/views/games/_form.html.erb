<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
    <span class="error"><%= @game.errors[:name].join('; ') %></span>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
    <span class="error"><%= @game.errors[:description].join('; ') %></span>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
